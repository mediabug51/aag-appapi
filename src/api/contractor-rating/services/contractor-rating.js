'use strict';

/**
 * contractor-rating service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contractor-rating.contractor-rating');
